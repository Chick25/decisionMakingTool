name: todolist
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  pie_chart: ^5.4.0
  stack_chart: 1.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.12        # ← Thêm dòng này
  hive_generator: ^2.0.1
  # shared_preferences: ^2.3.0

flutter:
  uses-material-design: true
